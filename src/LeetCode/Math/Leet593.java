package LeetCode.Math;

/**
 * @auther: Li jx
 * @date: 2019/4/27 16:16
 * @description:
 */
public class Leet593 {
//    public boolean validSquare(int[] p1, int[] p2, int[] p3, int[] p4) {
//        int a = (int) (Math.pow(p1[0] - p3[0], 2) + Math.pow(p3[1] - p1[1],2));
//        int b = (int) (Math.pow(p2[0] - p4[0], 2) + Math.pow(p4[1] - p2[1], 2));
//        int c = (int) (Math.pow(p1[0] - p2[0], 2) + Math.pow(p2[1] - p1[1],2));
//        return a == b && 2 * c == a;
//    }
}
